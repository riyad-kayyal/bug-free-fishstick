import 'package:flutter/material.dart';
// We name the class exampleModel, and we build an instructor.
class UserModel {
  final int id;
  final String name;
  final String phone;

  UserModel({
    required this.id,
    required this.name,
    required this.phone,
  }); // UserModel ( constructor)
} //class UserModel